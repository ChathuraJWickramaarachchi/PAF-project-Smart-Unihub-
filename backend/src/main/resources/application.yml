spring:
  application:
    name: smartuni-portal
  
  # MongoDB Configuration
  data:
    mongodb:
      uri: mongodb+srv://himashananayakkara08_db_user:I15Rf5Ustnrz2Gct@cluster0.1mnxjfz.mongodb.net/smart_campus_db?retryWrites=true&w=majority&appName=Cluster0
      database: smart_campus_db
  
  # File Upload Configuration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  
  # Email Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:dasanayakadushan@gmail.com}
    password: ${EMAIL_PASSWORD:dhwqmahyolzzcbnz}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp
  
  # OAuth2 Configuration
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:400294214813-m9kj6polprom5i6kvb770imsd1pkso87.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET: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}
            redirect-uri: "{baseUrl}/login/oauth2/code/google"
            scope:
              - email
              - profile
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com
          jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs

# Google OAuth Configuration
google:
  client:
    id: ${GOOGLE_CLIENT_ID:400294214813-m9kj6polprom5i6kvb770imsd1pkso87.apps.googleusercontent.com}

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /api

# Logging Configuration
logging:
  level:
    root: INFO
    com.smartcampus: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
